@using Klimatogrammen.ViewModels
@model Klimatogrammen.ViewModels.VragenIndexViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>VraagViewModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @foreach (VraagViewModel vvm in Model.VraagViewModels)
        {
            <div class="form-group">
                @Html.Label(vvm.VraagTekst, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.DropDownList(vvm.Antwoord, vvm.Antwoorden, htmlAttributes: new{@class="form-control"} )
                    @Html.ValidationMessage(vvm.Antwoord, "", new { @class = "text-danger" })
                </div>
            </div>

            @*<div class="form-group">
                @Html.Label("Antwoorden", htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.DropDownList(vvm.Antw.Antwoord, vvm.Antwoorden, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessage(vvm.VraagTekst, "", new { @class = "text-danger" })
                </div>
            </div>*@
        };

        @*<div class="form-group">
                @Html.LabelFor(model => model.ValidatieTekst, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.ValidatieTekst, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.ValidatieTekst, "", new { @class = "text-danger" })
                </div>
            </div>*@

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
